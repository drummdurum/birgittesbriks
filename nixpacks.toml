[phases.build]
cmds = [
  "npm ci --include=dev --ignore-scripts",
  "npm run build-css",
  "npm test",
  "npm prune --omit=dev"
]

[phases.start]
cmd = "npm run start:with-migrate"

[variables]
NODE_ENV = "production"